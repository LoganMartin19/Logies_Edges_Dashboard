import React from "react";

const Recommendation = ({ fixtureId }) => {
  // for now just placeholder, you can fetch per-market explain too
  return (
    <div className="card recommendation">
      <strong>Recommendation:</strong> Bet Over only if odds &gt; 1.43
    </div>
  );
};

export default Recommendation;